type User {
  id: ID!
  email: String
  name: String
  jwt: String
  createdAt: String
  updatedAt: String
}

type Mutation {
  login(email: String!, password: String!): User
  addUser(email: String!, password: String!, name: String!): User
}
